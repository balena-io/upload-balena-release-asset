import type * as BalenaSdk from '..';
import type { AtLeast } from '../../typings/utils';
export declare const LOCAL_MODE_ENV_VAR = "RESIN_SUPERVISOR_LOCAL_MODE";
export declare const LOCAL_MODE_SUPPORT_PROPERTIES: readonly ["os_version", "os_variant", "supervisor_version", "last_connectivity_event"];
export declare const checkLocalModeSupported: (device: Pick<BalenaSdk.Device, (typeof LOCAL_MODE_SUPPORT_PROPERTIES)[number]>) => void;
export declare const getLocalModeSupport: (device: AtLeast<BalenaSdk.Device, (typeof LOCAL_MODE_SUPPORT_PROPERTIES)[number]>) => {
    supported: boolean;
    message: string;
};
