import type * as BalenaSdk from '..';
import type * as DeviceState from '../types/device-state';
export declare const isProvisioned: (device: Partial<Pick<BalenaSdk.Device, "supervisor_version" | "last_connectivity_event">>) => boolean;
/**
 *
 * @param targetState
 * @returns array containing all images for all services for all releases for all apps for the device
 */
export declare const listImagesFromTargetState: (targetState: DeviceState.DeviceStateV3) => string[];
